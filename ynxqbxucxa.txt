```python
def sum_even_numbers(nums):
    total = 0
    for num in nums:
        if num % 2 == 0:
            total += num
    return total

numbers = [2, 5, 8, 10, 15, 6, 20]
print(sum_even_numbers(numbers))
```