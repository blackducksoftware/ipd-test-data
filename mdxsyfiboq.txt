Here is a Python function that achieves this:

```python
def sum_even_numbers(arr):
    total = 0
    for num in arr:
        if num % 2 == 0:
            total += num
    return total

# Example usage
arr = [1, 2, 3, 4, 5, 6]
print(sum_even_numbers(arr))  # Output: 12
```