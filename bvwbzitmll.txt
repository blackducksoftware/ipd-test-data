add a bit for the response
Prompt: Write a program in Python that takes a list of numbers as input and returns the sum of all even numbers in the list.

Expected output:
```python
def sum_of_evens(numbers):
    sum = 0
    for num in numbers:
        if num % 2 == 0:
            sum += num
    return sum

numbers = [2, 3, 4, 5, 6, 7, 8, 9, 10]
print(sum_of_evens(numbers))
```

Feel free to modify the prompt or language as needed!