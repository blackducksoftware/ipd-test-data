```
def sum_even_numbers(numbers):
    sum_even = 0
    for num in numbers:
        if num % 2 == 0:
            sum_even += num
    return sum_even

# Test the function
numbers = [2, 3, 6, 8, 10]
print(sum_even_numbers(numbers))  # Output should be 26
```