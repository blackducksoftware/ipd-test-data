add a bit for the response
Sure, here is a prompt you can ask LLM to generate code for:

"Write code in Python that takes in a list of numbers and returns a new list containing only the even numbers in the original list, sorted in descending order."