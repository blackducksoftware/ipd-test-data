add a bit for the response
Prompt: Write a program in Python that takes a list of numbers as input, sorts them in ascending order, removes duplicates, and then calculates the average of the remaining numbers.